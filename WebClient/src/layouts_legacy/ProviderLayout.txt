import { useEffect, useState } from "react";
import { Link, useLocation, useNavigate } from "react-router-dom";
import useAuthStore from "../store/authStore";
import "../assets/css/admin-custom.css";

const ProviderLayout = ({ children }) => {
  const location = useLocation();
  const navigate = useNavigate();
  const { user, logout } = useAuthStore();
  const [isSidebarCollapsed, setIsSidebarCollapsed] = useState(() => {
    if (typeof window === "undefined") return false;
    return window.innerWidth < 992;
  });
  const [isUserMenuOpen, setIsUserMenuOpen] = useState(false);
  const [openMenus, setOpenMenus] = useState({});

  useEffect(() => {
    // Add AdminLTE CSS
    if (!document.getElementById("adminlte-css")) {
      const link = document.createElement("link");
      link.rel = "stylesheet";
      link.href =
        "https://cdn.jsdelivr.net/npm/admin-lte@3.2/dist/css/adminlte.min.css";
      link.id = "adminlte-css";
      document.head.appendChild(link);
    }

    // Add FontAwesome
    if (!document.getElementById("fa-css")) {
      const fa = document.createElement("link");
      fa.rel = "stylesheet";
      fa.href =
        "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css";
      fa.id = "fa-css";
      document.head.appendChild(fa);
    }

    // Add layout class to body
    document.body.classList.add("admin-layout", "layout-fixed", "sidebar-mini");

    return () => {
      // Remove AdminLTE CSS & JS
      const existingLink = document.getElementById("adminlte-css");
      if (existingLink) document.head.removeChild(existingLink);

      const existingFa = document.getElementById("fa-css");
      if (existingFa) document.head.removeChild(existingFa);

      document.body.classList.remove(
        "admin-layout",
        "layout-fixed",
        "sidebar-mini",
        "sidebar-collapse",
      );
    };
  }, []);

  useEffect(() => {
    document.body.classList.toggle("sidebar-collapse", isSidebarCollapsed);
  }, [isSidebarCollapsed]);

  useEffect(() => {
    setIsUserMenuOpen(false);
    if (window.innerWidth < 992) {
      setIsSidebarCollapsed(true);
    }
  }, [location.pathname]);

  const handleLogout = () => {
    logout();
    navigate("/login");
  };

  const toggleMenu = (menuKey) => {
    setOpenMenus((prev) => ({
      ...prev,
      [menuKey]: !prev[menuKey],
    }));
  };

  const isActive = (path) => (location.pathname === path ? "active" : "");

  return (
    <div
      className={`wrapper admin-shell ${isSidebarCollapsed ? "sidebar-collapsed" : "sidebar-expanded"}`}
    >
      {/* Navbar */}
      <nav className="main-header navbar navbar-expand navbar-white navbar-light">
        <ul className="navbar-nav">
          <li className="nav-item">
            <button
              className="nav-link"
              type="button"
              aria-label="An hien sidebar"
              aria-expanded={!isSidebarCollapsed}
              onClick={() => setIsSidebarCollapsed((current) => !current)}
            >
              <i className="fas fa-bars"></i>
            </button>
          </li>
          <li className="nav-item d-none d-sm-inline-block">
            <Link to="/" className="nav-link text-primary font-weight-bold">
              <i className="fas fa-external-link-alt mr-1"></i> Xem Website
            </Link>
          </li>
        </ul>

        <ul className="navbar-nav ml-auto">
          <li className={`nav-item dropdown ${isUserMenuOpen ? "show" : ""}`}>
            <button
              className="nav-link"
              type="button"
              aria-expanded={isUserMenuOpen}
              onClick={() => setIsUserMenuOpen((current) => !current)}
            >
              <i className="far fa-user"></i>{" "}
              {user?.name || user?.username || "User"}
            </button>
            <div
              className={`dropdown-menu dropdown-menu-right ${isUserMenuOpen ? "show" : ""}`}
            >
              <span className="dropdown-item dropdown-header">
                {user?.email || "No email"}
              </span>
              <div className="dropdown-divider"></div>
              <button className="dropdown-item" onClick={handleLogout}>
                <i className="fas fa-sign-out-alt mr-2"></i> Đăng xuất
              </button>
            </div>
          </li>
        </ul>
      </nav>

      {/* Sidebar */}
      <aside className="main-sidebar sidebar-dark-primary elevation-4">
        <Link to="/provider" className="brand-link">
          <span className="brand-text font-weight-light ml-3">
            <b>ezTravel.</b> NCC
          </span>
        </Link>

        <div className="sidebar">
          <nav className="mt-2">
            <ul
              className="nav nav-pills nav-sidebar flex-column"
              data-widget="treeview"
              role="menu"
            >
              <li
                className={`nav-item has-treeview ${openMenus["overview"] ? "menu-open" : ""}`}
              >
                <a
                  className={`nav-link ${isActive("/provider") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("overview");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-tachometer-alt"></i>
                  <p>
                    Tổng quan
                    <i
                      className={`right fas ${openMenus["overview"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider"
                      className={`nav-link ${isActive("/provider") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Dashboard</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/operations"
                      className={`nav-link ${isActive("/provider/operations") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Today Operations</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/action-center"
                      className={`nav-link ${isActive("/provider/action-center") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Action Center</p>
                    </Link>
                  </li>
                </ul>
              </li>

              <li
                className={`nav-item has-treeview ${openMenus["services"] ? "menu-open" : ""}`}
              >
                <a
                  className={`nav-link ${isActive("/provider/services") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("services");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-concierge-bell"></i>
                  <p>
                    Dịch vụ
                    <i
                      className={`right fas ${openMenus["services"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider/services"
                      className={`nav-link ${isActive("/provider/services") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>All Services</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/services/new"
                      className={`nav-link ${isActive("/provider/services/new") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Create Service</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/services/quality"
                      className={`nav-link ${isActive("/provider/services/quality") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Listing Quality</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/services/amenities"
                      className={`nav-link ${isActive("/provider/services/amenities") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Amenities & Features</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/services/policies"
                      className={`nav-link ${isActive("/provider/services/policies") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Policies</p>
                    </Link>
                  </li>
                </ul>
              </li>

              <li
                className={`nav-item has-treeview ${openMenus["calendar"] ? "menu-open" : ""}`}
              >
                <a
                  className={`nav-link ${isActive("/provider/calendar") || isActive("/provider/inventory") || isActive("/provider/rate-availability") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("calendar");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-calendar-alt"></i>
                  <p>
                    Lịch & Inventory
                    <i
                      className={`right fas ${openMenus["calendar"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider/calendar"
                      className={`nav-link ${isActive("/provider/calendar") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Availability Calendar</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/rate-availability"
                      className={`nav-link ${isActive("/provider/rate-availability") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Rate & Availability Matrix</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/inventory"
                      className={`nav-link ${isActive("/provider/inventory") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Inventory / Capacity</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/time-slots"
                      className={`nav-link ${isActive("/provider/time-slots") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Time Slots</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/blackout"
                      className={`nav-link ${isActive("/provider/blackout") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Blackout Dates</p>
                    </Link>
                  </li>
                </ul>
              </li>

              <li
                className={`nav-item has-treeview ${openMenus["bookings"] ? "menu-open" : ""}`}
              >
                <a
                  className={`nav-link ${isActive("/provider/bookings") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("bookings");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-shopping-cart"></i>
                  <p>
                    Đơn đặt
                    <i
                      className={`right fas ${openMenus["bookings"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider/bookings"
                      className={`nav-link ${isActive("/provider/bookings") && !isActive("/provider/bookings/pending") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>All Bookings</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/bookings/pending"
                      className={`nav-link ${isActive("/provider/bookings/pending") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Pending Requests</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/bookings/kanban"
                      className={`nav-link ${isActive("/provider/bookings/kanban") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Booking Kanban</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/bookings/checkin"
                      className={`nav-link ${isActive("/provider/bookings/checkin") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Check-in / Attendance</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/bookings/refunds"
                      className={`nav-link ${isActive("/provider/bookings/refunds") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Cancellations & Refunds</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/bookings/requests"
                      className={`nav-link ${isActive("/provider/bookings/requests") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Special Requests</p>
                    </Link>
                  </li>
                </ul>
              </li>

              <li
                className={`nav-item has-treeview ${openMenus["pricing"] ? "menu-open" : ""}`}
              >
                <a
                  className={`nav-link ${isActive("/provider/pricing") || isActive("/provider/promotions") || isActive("/provider/coupons") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("pricing");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-tags"></i>
                  <p>
                    Giá & Khuyến mãi
                    <i
                      className={`right fas ${openMenus["pricing"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider/pricing"
                      className={`nav-link ${isActive("/provider/pricing") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Pricing Overview</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/pricing/rate-plans"
                      className={`nav-link ${isActive("/provider/pricing/rate-plans") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Rate Plans</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/promotions"
                      className={`nav-link ${isActive("/provider/promotions") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Promotions</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/coupons"
                      className={`nav-link ${isActive("/provider/coupons") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Coupon Codes</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/promotions/performance"
                      className={`nav-link ${isActive("/provider/promotions/performance") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Campaign Performance</p>
                    </Link>
                  </li>
                </ul>
              </li>

              <li
                className={`nav-item has-treeview ${openMenus["inbox"] ? "menu-open" : ""}`}
              >
                <a
                  className={`nav-link ${isActive("/provider/inbox") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("inbox");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-envelope"></i>
                  <p>
                    Inbox
                    <i
                      className={`right fas ${openMenus["inbox"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider/inbox"
                      className={`nav-link ${isActive("/provider/inbox") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Conversations</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/inbox/templates"
                      className={`nav-link ${isActive("/provider/inbox/templates") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Message Templates</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/inbox/automations"
                      className={`nav-link ${isActive("/provider/inbox/automations") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Automated Messages</p>
                    </Link>
                  </li>
                </ul>
              </li>

              <li
                className={`nav-item has-treeview ${openMenus["reviews"] ? "menu-open" : ""}`}
              >
                <a
                  className={`nav-link ${isActive("/provider/reviews") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("reviews");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-star"></i>
                  <p>
                    Reviews
                    <i
                      className={`right fas ${openMenus["reviews"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider/reviews"
                      className={`nav-link ${isActive("/provider/reviews") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>All Reviews</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/reviews/analytics"
                      className={`nav-link ${isActive("/provider/reviews/analytics") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Reputation Analytics</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/reviews/reported"
                      className={`nav-link ${isActive("/provider/reviews/reported") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Reported Reviews</p>
                    </Link>
                  </li>
                </ul>
              </li>

              <li
                className={`nav-item has-treeview ${openMenus["finance"] ? "menu-open" : ""}`}
              >
                <a
                  className={`nav-link ${isActive("/provider/finance") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("finance");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-wallet"></i>
                  <p>
                    Finance
                    <i
                      className={`right fas ${openMenus["finance"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider/finance"
                      className={`nav-link ${isActive("/provider/finance") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Finance Overview</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/finance/transactions"
                      className={`nav-link ${isActive("/provider/finance/transactions") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Transactions</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/finance/payouts"
                      className={`nav-link ${isActive("/provider/finance/payouts") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Payouts</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/finance/invoices"
                      className={`nav-link ${isActive("/provider/finance/invoices") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Invoices / Statements</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/finance/refunds"
                      className={`nav-link ${isActive("/provider/finance/refunds") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Refunds & Adjustments</p>
                    </Link>
                  </li>
                </ul>
              </li>

              <li
                className={`nav-item has-treeview ${openMenus["analytics"] ? "menu-open" : ""}`}
              >
                <a
                  className={`nav-link ${isActive("/provider/analytics") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("analytics");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-chart-line"></i>
                  <p>
                    Analytics
                    <i
                      className={`right fas ${openMenus["analytics"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider/analytics"
                      className={`nav-link ${isActive("/provider/analytics") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Overview</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/analytics/bookings"
                      className={`nav-link ${isActive("/provider/analytics/bookings") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Booking Analytics</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/analytics/revenue"
                      className={`nav-link ${isActive("/provider/analytics/revenue") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Revenue Analytics</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/analytics/occupancy"
                      className={`nav-link ${isActive("/provider/analytics/occupancy") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Occupancy / Utilization</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/analytics/visibility"
                      className={`nav-link ${isActive("/provider/analytics/visibility") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Search Visibility</p>
                    </Link>
                  </li>
                </ul>
              </li>

              <li
                className={`nav-item has-treeview ${openMenus["media"] ? "menu-open" : ""}`}
              >
                <a
                  className={`nav-link ${isActive("/provider/media") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("media");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-photo-video"></i>
                  <p>
                    Media
                    <i
                      className={`right fas ${openMenus["media"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider/media"
                      className={`nav-link ${isActive("/provider/media") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Media Library</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/media/gallery"
                      className={`nav-link ${isActive("/provider/media/gallery") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Gallery Manager</p>
                    </Link>
                  </li>
                </ul>
              </li>

              <li
                className={`nav-item has-treeview ${openMenus["locations"] ? "menu-open" : ""}`}
              >
                <a
                  className={`nav-link ${isActive("/provider/locations") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("locations");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-map-marker-alt"></i>
                  <p>
                    Locations
                    <i
                      className={`right fas ${openMenus["locations"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider/locations"
                      className={`nav-link ${isActive("/provider/locations") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Location Profile</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/locations/pickup"
                      className={`nav-link ${isActive("/provider/locations/pickup") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Pickup / Meeting Points</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/locations/area"
                      className={`nav-link ${isActive("/provider/locations/area") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Service Area</p>
                    </Link>
                  </li>
                </ul>
              </li>

              <li
                className={`nav-item has-treeview ${openMenus["team"] ? "menu-open" : ""}`}
              >
                <a
                  className={`nav-link ${isActive("/provider/team") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("team");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-users"></i>
                  <p>
                    Team
                    <i
                      className={`right fas ${openMenus["team"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider/team"
                      className={`nav-link ${isActive("/provider/team") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Members</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/team/roles"
                      className={`nav-link ${isActive("/provider/team/roles") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Roles & Permissions</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/team/activity"
                      className={`nav-link ${isActive("/provider/team/activity") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Activity Log</p>
                    </Link>
                  </li>
                </ul>
              </li>

              <li
                className={`nav-item has-treeview ${openMenus["settings"] ? "menu-open" : ""}`}
              >
                <a
                  className={`nav-link ${isActive("/provider/settings") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("settings");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-cog"></i>
                  <p>
                    Business Settings
                    <i
                      className={`right fas ${openMenus["settings"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider/settings"
                      className={`nav-link ${isActive("/provider/settings") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Business Profile</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/settings/verification"
                      className={`nav-link ${isActive("/provider/settings/verification") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Verification</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/settings/bank"
                      className={`nav-link ${isActive("/provider/settings/bank") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Bank Account</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/settings/tax"
                      className={`nav-link ${isActive("/provider/settings/tax") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Tax Information</p>
                    </Link>
                  </li>
                  <li className="nav-item">
                    <Link
                      to="/provider/settings/hours"
                      className={`nav-link ${isActive("/provider/settings/hours") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Operating Hours</p>
                    </Link>
                  </li>
                </ul>
              </li>

              <li className="nav-item has-treeview">
                <a
                  className={`nav-link ${isActive("/provider/support") ? "active" : ""}`}
                  onClick={(e) => {
                    e.preventDefault();
                    toggleMenu("support");
                  }}
                  style={{ cursor: "pointer" }}
                >
                  <i className="nav-icon fas fa-headset"></i>
                  <p>
                    Support
                    <i
                      className={`right fas ${openMenus["support"] ? "fa-angle-down" : "fa-angle-left"}`}
                    ></i>
                  </p>
                </a>
                <ul className="nav nav-treeview">
                  <li className="nav-item">
                    <Link
                      to="/provider/support"
                      className={`nav-link ${isActive("/provider/support") ? "active" : ""}`}
                    >
                      <i className="far fa-circle nav-icon"></i>
                      <p>Help Center</p>
                    </Link>
                  </li>
                </ul>
              </li>
            </ul>
          </nav>
        </div>
      </aside>

      {/* Content */}
      <div className="content-wrapper">
        <section className="content">{children}</section>
      </div>

      {/* Footer */}
      <footer className="main-footer">
        <strong>
          Copyright &copy; 2026 <a href="#">ezTravel</a>.
        </strong>
        <div className="float-right d-none d-sm-inline-block">
          <b>Phiên bản</b> 1.0.0
        </div>
      </footer>
    </div>
  );
};

export default ProviderLayout;
